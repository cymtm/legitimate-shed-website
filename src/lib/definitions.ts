export type Game = {
  slug: string;
  title: string;
  description: string;
  imageUrl: string;
  gameUrl: string;
  imageHint: string;
};

export type App = {
  slug: string;
  title: string;
  description: string;
  imageUrl: string;
  appUrl: string;
  imageHint: string;
};
